const testVar = {}

function testFunc() {
  return "hi"
}

function superbowlWin(arr) {
  arr.find(obj => {
    return obj.result === 'W'
  })
};

// this literally returns 'undefined' when testing in console... going to submit.